meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"

build:
  requirementsPath: requirements.txt

run:
  command: PYTHONPATH=backend alembic upgrade head && cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000
  containerPort: 8000
